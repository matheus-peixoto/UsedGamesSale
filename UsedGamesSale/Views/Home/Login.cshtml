@model UserLoginDTO

@{
    ViewData["Title"] = "Login";
}

<div class="login container">
    <div class="card">
        <form method="post" class="form">
            <div class="form-title">
                <h2 class="title-color">Sign In</h2>
            </div>

            @if (ViewData.ContainsKey("MSG_E"))
            {
                <div class="form-control">
                    <div class="msg-error">@ViewData["MSG_E"].ToString()</div>
                </div>
            }

            <div class="form-control">
                <input asp-for="Email" type="email" placeholder="Email">
                <div class="msg-error-sm"><span asp-validation-for="Email"></span></div>
            </div>
            <div class="form-control">
                <input id="password" asp-for="Password" type="password" placeholder="Password" maxlength="20" min="8">
                <div class="msg-error-sm"><span asp-validation-for="Password"></span></div>
            </div>
            <div class="flex mg-bottom-5px">
                <input id="showpasswordBtn" type="radio">
                <label for="showpasswordBtn">Show password</label>
            </div>
            <a href="" class="mg-bottom-5px">Forgot password?</a>
            <button type="submit" class="btn btn-block btn-primary">Login</button>
        </form>
    </div>
</div>

<script src="~/js/login.js"></script>